//bigger number among two number

#include <stdio.h>

main(){
    int x,y;
    printf("Enter two number:\n");
    scanf("%d %d",&x,&y);
    if(x>y) printf("%d is the bigger number\n",x);
    else printf("%d is the bigger number\n",y);
}
